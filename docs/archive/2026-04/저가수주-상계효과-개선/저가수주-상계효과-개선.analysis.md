# Gap Analysis: 저가수주 상계효과 개선

> Analysis Date: 2026-04-14
> Plan: `~/.claude/plans/memoized-watching-wadler.md`
> Implementation: `offsetEffect.ts`, `OffsetEffectTab.tsx`, `offsetEffect.test.ts`

## Match Rate: 100%

모든 Plan 항목(Phase 1~4, 총 16개 세부 항목)이 설계대로 구현됨.

## Phase별 검증 결과

| Phase | Score | Items |
|-------|:-----:|:-----:|
| Phase 1: 데이터 신뢰성 | 100% | 4/4 |
| Phase 2: 차트 & UX | 100% | 6/6 |
| Phase 3: 기능 확장 | 100% | 3/3 |
| Phase 4: 테스트 보강 | 100% | 9/9 (계획 7 + 보너스 2) |

## 상세 항목별 검증

### Phase 1: 데이터 신뢰성 경고 & 수학적 보완

| # | 항목 | 상태 | 근거 |
|---|------|:----:|------|
| 1-1 | isDateFiltered 경고 배너 | ✅ | OffsetEffectTab.tsx — 조건부 amber 배너, AlertTriangle 아이콘 |
| 1-2 | 4사분면 중앙값 피벗 | ✅ | offsetEffect.ts — `median()` 함수, 짝수 시 `(sorted[mid-1]+sorted[mid])/2` |
| 1-3 | 반품/환입 아이템 분리 | ✅ | CVPSummary에 `returnItemCount`/`returnRevenue` 추가, quadrant 분류 제외, UI 배지 |
| 1-4 | 품목코드 정규화 경고 | ✅ | `calcItemPool` → `warnings: string[]` 반환, UI 경고 표시 |

### Phase 2: 차트 & 테이블 UX 개선

| # | 항목 | 상태 | 근거 |
|---|------|:----:|------|
| 2-1 | CVP 차트 적응형 X축 | ✅ | `maxMultiplier = Math.max(2.2, effectiveFactor * 1.5)`, volumeAbsolute 포함 |
| 2-2 | Dog 테이블 확장 | ✅ | `showAllDogs` 토글, 전체 건수 표시 |
| 2-3 | Pool Impact 테이블 확장 | ✅ | `showAllPool` 토글, 11건 → 전체 |
| 2-4 | Scatter 툴팁 전체 이름 | ✅ | `fullItemName` 필드, `max-w-[300px] break-words` |
| 2-5 | 프리셋 모드 보존 | ✅ | 프리셋 버튼에서 `setInputMode` 제거 (초기화 제외) |
| 2-6 | 빈 Pool 안내 + 정규화 경고 | ✅ | 빈 풀 메시지 + poolWarnings 건수 표시 |

### Phase 3: 기능 확장

| # | 항목 | 상태 | 근거 |
|---|------|:----:|------|
| 3-1 | CSV 내보내기 | ✅ | Dog 테이블에 ExportButton (action prop) |
| 3-2 | 감도 분석 연동 | ✅ | `calcSensitivityGrid` (binary search 15회), 미니 테이블 UI |
| 3-3 | 시나리오 비교 | ✅ | `savedScenarios` state (최대 3), 비교 테이블 + 초기화 |

### Phase 4: 테스트 보강

| # | 테스트 케이스 | 상태 |
|---|-------------|:----:|
| a | 짝수 개 중앙값 피벗 대칭성 | ✅ |
| b | 음수 수량 아이템 분류 + summary | ✅ |
| c | volumeAbsolute 모드 풀 시뮬 | ✅ |
| d | 단가 인상 워터폴 (green bar) | ✅ |
| e | baseOP=0 integrity 엣지케이스 | ✅ |
| f | getUnitGroups 2개 미만 필터링 | ✅ |
| g | calcGroupCVP BEP 수량 | ✅ |
| h | calcSensitivityGrid 역산 | ✅ |
| i | 코드 정규화 경고 수집 | ✅ |

## 설계 외 추가 구현 (보너스)

- `absFactor`: volumeAbsolute 모드에서도 X축 범위 적응
- `priceChangeDirect` 동기화: 프리셋 클릭 시 absolute 모드 단가 입력도 동시 갱신

## 빌드 & 테스트 검증

| 검증 | 결과 |
|------|------|
| `npm run test` (offsetEffect) | 38/38 통과 |
| `npm run build` | 성공 (0 errors) |
| 항등식 검증 (6 시나리오) | 모두 일관성 유지 |

## 미구현 항목 (의도적 제외)

- Phase 5 (성능 최적화): 계획서에 "Priority: Low"로 명시 → 향후 별도 진행
