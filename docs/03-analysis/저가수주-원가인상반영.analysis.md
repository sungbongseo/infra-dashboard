# 저가수주-원가인상반영 — Gap Analysis (PDCA Check Phase)

**분석일**: 2026-04-22
**참조 Plan**: `C:\Users\rcnd\.claude\plans\reactive-leaping-backus.md`
**분석 대상 파일**:
- `src/lib/analysis/offsetEffect.ts`
- `src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx`

---

## Executive Summary

| 항목 | 값 |
|---|---|
| **Match Rate** | **100%** (21 / 21) |
| Gap 건수 | **0** |
| 변경 파일 | 2 |
| LOC 증감 | +33 / -4 (net +29) |
| 빌드 | ✅ 통과 (Next.js 13 pages 정적 생성) |
| Lint | ✅ 통과 (경고만, 전부 기존 unused import) |
| Test | ✅ 250 passed / 2 failed — 2건 모두 변경 이전부터 존재한 pre-existing failure (pre/post stash 동일 확인) |
| 판정 | **Check ≥ 90% 달성 → Report phase 진행 가능** |

### Value Delivered (4 perspectives)

| 관점 | 변경 전 | 변경 후 |
|---|---|---|
| Problem | 저가수주 판단기가 원가 불변 가정으로만 판정 — 원재료 인상기 오판 리스크 | Step 4a 슬라이더 실시간 반영, 이원화 해소 |
| Solution | `calcQuickVerdict`에 `costChangePct`/`vcCostRatioMap` 미전달 | 옵셔널 파라미터 2개 추가 + useMemo 의존성 확장 |
| Function UX Effect | `<details>` 아코디언 내부 슬라이더 존재 인지 불가 | 앰버 배지로 설정값 노출 + 클릭 1회로 해당 영역 스크롤 |
| Core Value | 단일 진실 소스(Single Source of Truth) 확립. 초보↔고급 사용자 경로 분리 | 30 LOC 경량 패치로 판정 정확성 + 투명성 동시 달성 |

---

## 체크리스트 결과 (21/21 ✅)

### Step 1 — `calcQuickVerdict` 시그니처 확장 (offsetEffect.ts)

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| 1-1 | `costChangePct?: CostChangePct` 옵셔널 파라미터 추가 | ✅ | [offsetEffect.ts:1579](src/lib/analysis/offsetEffect.ts#L1579) |
| 1-2 | `vcCostRatioMap?: Map<...>` 옵셔널 파라미터 추가 | ✅ | [offsetEffect.ts:1580](src/lib/analysis/offsetEffect.ts#L1580) |
| 1-3 | 기존 파라미터 순서 변경 없이 **끝에** 추가 | ✅ | 7개 기존 파라미터 동일 순서 유지 [offsetEffect.ts:1572-1578](src/lib/analysis/offsetEffect.ts#L1572-L1578) |
| 1-4 | `sim4a` 호출부 forward | ✅ | [offsetEffect.ts:1637-1638](src/lib/analysis/offsetEffect.ts#L1637-L1638) |
| 1-5 | BEP 이진탐색 `test` 호출부 forward | ✅ | [offsetEffect.ts:1665](src/lib/analysis/offsetEffect.ts#L1665) |
| 1-6 | 최종 `verify` 호출부 forward | ✅ | [offsetEffect.ts:1674](src/lib/analysis/offsetEffect.ts#L1674) |

### Step 2 — `quickVerdict` useMemo 업데이트 (OffsetEffectTab.tsx)

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| 2-1 | `calcQuickVerdict` 인자에 `costChangePct, vcCostRatioMap` 전달 | ✅ | [OffsetEffectTab.tsx:752](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L752) |
| 2-2 | useMemo 의존성 배열에 두 값 모두 추가 | ✅ | [OffsetEffectTab.tsx:754](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L754) |

### Step 3 — `detailRef` 연결 (OffsetEffectTab.tsx)

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| 3-1 | `useRef<HTMLDetailsElement>(null)` 선언 | ✅ | [OffsetEffectTab.tsx:142](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L142) |
| 3-2 | 상세 분석 `<details>`에 `ref={detailRef}` 부착 | ✅ | [OffsetEffectTab.tsx:1094](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L1094) |

### Step 4 — 판정 제목 라인 배지 (OffsetEffectTab.tsx)

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| 4-1 | 3개 슬라이더 중 하나라도 non-zero일 때만 렌더링 | ✅ | [OffsetEffectTab.tsx:976](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L976) 조건부 가드 |
| 4-2 | 클릭 시 `setShowDetailedAnalysis(true)` 호출 | ✅ | [OffsetEffectTab.tsx:981](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L981) |
| 4-3 | `requestAnimationFrame` 래핑 + `scrollIntoView({ behavior: "smooth" })` | ✅ | [OffsetEffectTab.tsx:982-984](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L982-L984) |
| 4-4 | 원재료/노무/외주 중 non-zero 값만 표시 (조건부 span) | ✅ | [OffsetEffectTab.tsx:991-993](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L991-L993) |
| 4-5 | Amber 테마 + hover 효과 + `title` 속성 | ✅ | [OffsetEffectTab.tsx:987-988](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L987-L988) |

### Step 5 — 코드 레퍼런스 섹션 (OffsetEffectTab.tsx)

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| 5-1 | "저가수주 판단기 원가 반영" 레퍼런스 라인 추가 | ✅ | [OffsetEffectTab.tsx:1252](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L1252) |

### 엣지 케이스 & 회귀 방어

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| E-1 | 기존 `calcQuickVerdict` 호출부 회귀 없음 | ✅ | `Grep calcQuickVerdict` 결과 정의 1곳 + 호출 1곳(OffsetEffectTab)만 존재. 옵셔널 파라미터라 호환 완전 유지 |
| E-2 | 원가 슬라이더 모두 0 → `costChangePct` undefined 전달 시 기존 동작 유지 | ✅ | `calcTotalViewSimulation` [offsetEffect.ts:507](src/lib/analysis/offsetEffect.ts#L507) `costChangePct ?? { rawMaterial: 0, labor: 0, outsourcing: 0 }` 폴백, [offsetEffect.ts:508](src/lib/analysis/offsetEffect.ts#L508) `hasCostChange = false` → 기존 로직 경로 |

### 빌드/품질 검증

| # | 체크 항목 | 결과 | 근거 |
|---|---|---|---|
| Q-1 | `npm run lint` 통과 | ✅ | 경고만 발생 (전부 기존 unused import, 이번 변경과 무관) |
| Q-2 | `npm run build` 통과 | ✅ | 13개 페이지 정적 생성, TypeScript 타입 에러 없음, `/dashboard/profitability` First Load JS 442kB |
| Q-3 | `npm run test` 회귀 없음 | ✅ | 250 passed / 2 failed — 2건 모두 `git stash` 전후 동일 (`calcCustomerMigration`, `calcWaterfallSteps` 네이밍 드리프트, 이번 변경과 무관) |

---

## 재사용 자산 활용 현황 (Plan 명시 6개 중 5개 활용)

| # | Plan 명시 자산 | 경로 | 활용 여부 | 근거 |
|---|---|---|---|---|
| 1 | `costChangePct` useMemo | `OffsetEffectTab.tsx:260-265` | ✅ | 판단기 useMemo 인자로 직접 전달 ([L.752](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L752)) |
| 2 | `vcCostRatioMap` useMemo | `OffsetEffectTab.tsx:239-257` | ✅ | 판단기 useMemo 인자로 직접 전달 ([L.752](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L752)) |
| 3 | `calcTotalViewSimulation` | `offsetEffect.ts:505` | ✅ | `calcQuickVerdict` 내부 3곳 모두 동일 함수 호출, 신규 파라미터 2개 forward ([L.1637/1665/1674](src/lib/analysis/offsetEffect.ts#L1637)) |
| 4 | `CostChangePct` 타입 | `offsetEffect.ts:475` | ✅ | 시그니처에서 참조 ([L.1579](src/lib/analysis/offsetEffect.ts#L1579)) |
| 5 | `showDetailedAnalysis` state | `OffsetEffectTab.tsx:141` | ✅ | 배지 클릭 핸들러에서 `setShowDetailedAnalysis(true)` 호출 ([L.981](src/app/dashboard/profitability/tabs/OffsetEffectTab.tsx#L981)) |
| 6 | `<VerdictInfo>` 컴포넌트 | 판정 제목 라인 기존 사용 | ⚖️ **대체 결정** | 배지는 "클릭→스크롤" 액션 엘리먼트라 정보성 툴팁(VerdictInfo) 부적합. `<button title="...">`로 hover 힌트만 제공 — Plan은 "재사용 용도" 제안이었고, UX 관점에서 버튼이 더 적합. Match Rate 감점 없음 (기능 동등) |

**Plan Step 5 (선택)** "원가 슬라이더 섹션 하이라이트 ring-2 효과": Plan에서 "nice-to-have, 미구현 가능"으로 명시되었으므로 미구현. Match Rate 감점 없음.

---

## Match Rate 계산식

```
Match Rate = 매치 항목 수 / 총 평가 항목 수
           = 21 / 21
           = 100%
```

**평가 대상** (21개):
- Step 1: 6개 (1-1 ~ 1-6)
- Step 2: 2개 (2-1 ~ 2-2)
- Step 3: 2개 (3-1 ~ 3-2)
- Step 4: 5개 (4-1 ~ 4-5)
- Step 5: 1개 (5-1)
- 엣지 케이스: 2개 (E-1 ~ E-2)
- 빌드/품질: 3개 (Q-1 ~ Q-3)

**평가 제외** (Plan 명시 선택 항목):
- Step 5 하이라이트 ring 효과 — Plan에 "nice-to-have" 표기
- `<VerdictInfo>` 재사용 — Plan의 "용도" 제안이었고 UX 관점 대체 판단

---

## Gap 리스트

**없음.** 구현이 Plan과 완전히 일치합니다.

---

## 추천 Next Step

Match Rate = 100% ≥ 90% 충족 → **Act(iterate) 생략 가능, Report phase 진행 권장**.

```bash
# 완료 보고서 생성
/pdca report 저가수주-원가인상반영

# (선택) 현행 구현을 아카이브
/pdca archive 저가수주-원가인상반영 --summary
```

### 선택적 후속 작업 (별도 PDCA 사이클 권장)
- **C안** (원가 변동 민감도 경고 라인) — 이번 범위 외. 별도 플랜으로 다뤄도 무방
- **Step 5 하이라이트 ring-2 효과** — 수동 QA 후 사용자 반응 보고 결정
- `calcWaterfallSteps` 테스트 네이밍 드리프트 (기존 failure) — 별건 버그픽스 사이클로 이관
