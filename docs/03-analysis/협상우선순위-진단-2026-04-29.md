# 협상 우선순위 탭 정밀 진단 + 개선 보고서

**작성일**: 2026-04-29
**범위**: NegotiationPriorityTab 중복 표시 / 숫자 정확성 / 라벨 일관성
**판정**: 🟢 **4 이슈 모두 해결 + 14 단위 테스트 100% 통과 + 빌드 0 errors**

---

## 1. 발견 경위

사용자가 미수금 페이지 → 협상 우선순위 탭에서 "리본" 검색 시 동일 거래처가 **#1, #2 2건 중복 표시**되는 것을 발견. 추가로 다음 의심점 확인 요청:
- 미수금 138,828,895원 vs Python raw 분석 151,344,230원 (12.5M 차이)
- 위험점수 40 vs Python dry-run 43
- "13M 적자: +8,905,265" 라벨이 양수에 "적자" 표기

**왜 중요한가**: 회수 우선순위 결정의 신뢰도 = 협상 신뢰도. 대시보드 자체에 모순이 있으면 영업이 데이터를 신뢰하지 못함.

---

## 2. 진단 결과 (4 이슈 — 전수 확정)

### 🚨 이슈 1 — 거래처 중복 표시 (HIGH, 코드 버그)

**위치**: `src/lib/analysis/customerCompositeRisk.ts:509-525`

**원인**:
```ts
// 두 출처에서 키를 정규화 없이 Set에 추가
allCustomerCodes.add(k)  // L511: aging 코드 ("C100002775")
allCustomerCodes.add(k)  // L520: 100 손익 매출거래처 (이름 "리본TS")
```
같은 거래처가 코드 + 이름 두 키로 Set에 들어감 → loop에서 두 번 처리 → fallback fuzzy 매칭으로 같은 데이터에 도달 → **동일 metrics를 갖는 risk 객체 2개 생성**.

**수정**: 100 데이터 키를 aging 코드로 정규화 후 Set에 추가 (정확 매칭 + 부분 일치 fallback 동시 정규화).

### ⚠ 이슈 2 — 미수금 12.5M 차이 (MEDIUM, 파일 관리)

**원인**: 같은 파일명이 두 경로에 다른 내용으로 존재.

| 경로 | 미수 | 출처 |
|---|---|---|
| `./업로드자료/건자재_미수채권연령.xlsx` | **138,828,895** | 대시보드 (현 truth) |
| `./기타/업로드자료/건자재_미수채권연령.xlsx` | 151,344,230 | Python (구버전 백업) |

**검증** (3 method 모두 일치):
- Method A (월별 7개 bucket 합계 = col 7+10+13+16+19+22+25) = 138,828,895
- Method B (col 29 합계 장부금액 직접) = 138,828,895
- Method C (col 28 합계 거래금액) = 138,828,895

→ **알고리즘 정확**. 단순히 파일 버전 차이.

**수정**: Python dry-run 스크립트의 `DATA_DIR`을 `./업로드자료/` 로 변경. 이전 폴더에 archive README 추가.

### ⚠ 이슈 3 — 위험점수 40 vs 43 (MEDIUM, 정렬 필요)

| 항목 | 대시보드 (TS) | Python dry-run (변경 전) |
|---|---|---|
| 미수 | 138M (현 truth) | 151M (구파일) |
| 장기연체 정의 | month6 + overdue (6M+) = 64M | (이미 같음 — 코드 변경 안 함) |
| 위험점수 | **40** | 43 |

**원인**: Python 스크립트가 다른 파일을 읽어서 다른 입력 → 다른 점수.

**수정 후 검증**:
```
[검증] 알려진 위험 거래처 위치 확인:
  대성    : #1 (대성이앤씨 주식회사, 점수 75, 회수+단가)
  건진    : #2 (건진케미컬, 점수 62, 회수+단가)
  리본    : #19 (리본TS, 점수 40, 단가조정)  ✅ 대시보드 정확히 일치
```

### ⚠ 이슈 4 — "13M 적자: +X" 라벨 모순 (LOW, UX)

**위치**: `src/app/dashboard/receivables/tabs/NegotiationPriorityTab.tsx:128-131`

**원인**: 라벨이 항상 "13M 적자"인데 양수일 때 "+" 부호 표시 = 자기 모순.

**수정**: 조건부 라벨 + "+" 부호 자동 처리.
```tsx
{m.totalProfit13M < 0 ? "13M 적자" : "13M 영업이익"}
{formatCurrency(m.totalProfit13M)}  // formatCurrency가 부호 처리
```

추가로 "장기연체" → **"장기연체 (6M+)"** 로 기간 명시.

---

## 3. 사용자 결정 사항 (확정)

| 항목 | 결정 | 영향 |
|---|---|---|
| 장기연체 정의 | 6M+ 유지 | 코드 변경 없음, UI 라벨 명시 |
| 파일 truth | `./업로드자료/` (138M) | `./기타/업로드자료/`는 archive |
| Python 정렬 | 지금 즉시 | dry-run script DATA_DIR 변경 + 결과 일치 검증 |

---

## 4. 변경 사항 (5 파일)

| 파일 | 변경 |
|---|---|
| `src/lib/analysis/customerCompositeRisk.ts` | L519-540: 거래처 코드 정규화 + dedup (+18 LOC) |
| `src/app/dashboard/receivables/tabs/NegotiationPriorityTab.tsx` | L128-138: 13M 라벨 조건부 + 장기연체 (6M+) 명시 |
| `src/lib/analysis/customerCompositeRisk.test.ts` | 5 신규 테스트 추가 (+90 LOC) |
| `scripts/dry-run-composite-risk.py` | DATA_DIR 변경 |
| `기타/업로드자료/_ARCHIVE_README.md` | 신규 — archive 명시 + 점진 정리 계획 |

---

## 5. 단위 테스트 결과

```
RUN  v4.0.18

✓ src/lib/analysis/customerCompositeRisk.test.ts (14 tests) 14ms

Test Files  1 passed (1)
     Tests  14 passed (14)
```

**14개 테스트 100% 통과** — 기존 9개 + 신규 5개.

신규 5개 테스트 상세:
1. ✅ `aging에만 있는 거래처 → 1건`
2. ✅ `100 손익에만 있는 거래처 → 1건 (aging 없음)`
3. ✅ `코드(aging) + 이름(100) 동시 등장 시 1건으로 dedup (리본TS 케이스)` ⭐ 핵심
4. ✅ `부분 일치 거래처 (대성이앤씨 vs 대성이앤씨 주식회사) → 1건`
5. ✅ `같은 코드에 multiple aging 행 (일성: 김승욱 + 이승현) → 1건 합산`

---

## 6. 빌드 검증

```
✓ Generating static pages (13/13)
   Finalizing page optimization ...
Route (app)                              Size     First Load JS
├ ○ /dashboard/receivables               7.29 kB         304 kB
```

**TypeScript 0 errors, ESLint 0 errors, Static prerender 13/13 pages** — 회귀 없음.

---

## 7. Python dry-run 정렬 결과

| 거래처 | 변경 전 | 변경 후 (대시보드 일치) |
|---|---|---|
| 대성이앤씨 | 75점 | 75점 (변동 없음 — 다른 파일에서도 같은 점수) |
| 건진케미컬 | 73점 | 62점 (구파일에서 13M 적자 더 컸음 → 이번 데이터로 보정) |
| **리본TS** | **43점** | **40점 ✅ 대시보드 일치** |

→ 대시보드와 Python 결과 정합성 확보.

---

## 8. Verification Matrix (전/후 비교)

### 리본TS 검색 시

| 항목 | Before | After |
|---|---|---|
| 표시 건수 | 2건 (중복) | **1건** ✅ |
| 미수 | 138,828,895 | 138,828,895 (변동 없음) |
| 장기연체 라벨 | "장기연체" | **"장기연체 (6M+)"** ✅ |
| 13M 영업이익 표시 | "13M 적자: +8,905,265" (모순) | **"13M 영업이익: ₩8,905,265"** ✅ |
| 위험점수 (TS) | 40 | 40 (변동 없음) |
| 위험점수 (Python) | 43 (다른 파일) | **40 (대시보드 일치)** ✅ |

### 일성 검색 시 예상

| 항목 | Before | After |
|---|---|---|
| 표시 건수 | 2건 (코드/이름 중복) | **1건** ✅ |
| 다중 담당자 (김승욱+이승현) | 합산 표시 | 합산 + offices 배열 (변동 없음) |

### 대성이앤씨 검색 시 (회귀 검증)

| 항목 | Before | After |
|---|---|---|
| 표시 건수 | 1건 (이미 정상) | **1건 유지** ✅ |
| 부분 일치 매칭 | 동작 중 | **정규화 단계로 이동, 동작 유지** ✅ |

---

## 9. 잔여 작업 (운영 정책)

### 단기 (1주 내)

- [ ] 사용자 수동 검증: dev 서버에서 리본TS / 일성 검색 → 1건 표시 확인
- [ ] PDF Bulk export 회귀 — 5건 선택 후 출력 시 카드 5장 (중복 카드 없음)

### 중기 (1-2개월)

- [ ] 다른 6개 스크립트의 DATA_DIR 일괄 정리 (`./업로드자료/` 통일)
- [ ] `./기타/업로드자료/` 폴더 완전 삭제 검토

### 장기 (D+60 운영 추적)

- [ ] silent-risk-detection 계획서 Decision Gate 평가 시 본 진단의 dedup 효과를 운영 데이터로 검증

---

## 10. 결론

| 질문 | 답변 |
|---|---|
| **중복 버그 해결?** | 🟢 코드 정규화 + 5개 단위 테스트로 회귀 방지 |
| **숫자 정확?** | 🟢 알고리즘 검증 완료, 파일 버전 차이는 archive 분리로 해결 |
| **라벨 일관성?** | 🟢 13M 적자/영업이익 조건부 + 장기연체 (6M+) 명시 |
| **Python 정합성?** | 🟢 대시보드와 점수 일치 (리본TS 40점) |
| **회귀 위험?** | 🟢 빌드 0 errors + 14 테스트 통과 + 기존 fallback 매칭 유지 |

### 핵심 메시지

> **사용자가 발견한 1건의 중복 버그 + 3건의 부가 의심점이 모두 진단되어 해결되었습니다. 협상 우선순위 탭은 이제 거래처당 1건만 표시하며, 라벨/숫자/Python dry-run이 자기 일관성을 갖춥니다. 회수 의사결정의 신뢰 기반이 회복되었습니다.**

---

**검증 데이터**:
- 단위 테스트: `npm run test customerCompositeRisk` (14/14)
- 빌드: `npm run build` (0 errors)
- Python: `python scripts/dry-run-composite-risk.py` (리본TS 40점 일치)
- 수동: dev 서버 → 리본 검색 → 1건 표시 (사용자 검증 대기)

**다음 액션**: 사용자가 dev 서버에서 수동 회귀 확인 후 commit + push.
